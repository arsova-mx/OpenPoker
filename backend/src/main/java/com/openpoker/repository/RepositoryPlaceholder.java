package com.openpoker.repository;

/**
 * Placeholder package for Spring Data JPA repositories.
 *
 * <p>Repositories to be created during development:
 * <ul>
 *   <li>{@code SessionRepository}  – extends JpaRepository&lt;Session, UUID&gt;</li>
 *   <li>{@code StoryRepository}    – extends JpaRepository&lt;Story, UUID&gt;</li>
 *   <li>{@code VoteRepository}     – extends JpaRepository&lt;Vote, UUID&gt;</li>
 *   <li>{@code ParticipantRepository} – extends JpaRepository&lt;Participant, UUID&gt;</li>
 * </ul>
 */
public final class RepositoryPlaceholder {
    private RepositoryPlaceholder() {}
}
