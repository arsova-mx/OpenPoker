/**
 * HelloWorld – temporary landing component.
 *
 * Confirms the React + Vite + TypeScript setup is working.
 * Will be replaced by the actual OpenPoker UI during development.
 */
function HelloWorld() {
  return (
    <section style={{ textAlign: 'center', marginTop: '4rem', fontFamily: 'sans-serif' }}>
      <h1>🃏 OpenPoker</h1>
      <p>Hello from OpenPoker! The frontend is up and running.</p>
      <p style={{ color: '#888', fontSize: '0.9rem' }}>
        React · Vite · TypeScript
      </p>
    </section>
  );
}

export default HelloWorld;
